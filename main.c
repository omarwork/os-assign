#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <unistd.h>
char *read_line();
void parse(char* preparse);

int main()
{
    char * ahma;
    printf("Hello world!\n");
    //ahma=read_line();
    //puts(ahma);
    //printf("\n%d",strlen(ahma));
    //parse(ahma);

    return 0;
}
